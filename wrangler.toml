name = "invite-maker"
compatibility_date = "2026-08-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "public"

# Pages 项目 + Pages Functions（functions/ 目录自动编译成 Worker）
# 前端静态：  public/
# 后端 API：  functions/api/render.js  →  POST /api/render
#
# 本地预览：  pnpm dev   (wrangler pages dev)
# 部署上线：  pnpm deploy
