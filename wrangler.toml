name = "invite-maker"
compatibility_date = "2026-08-01"
pages_build_output_dir = "public"

# 纯静态 Pages 项目：public/ 目录直接部署，无需构建。
# 本地预览：  npx wrangler pages dev public
# 部署上线：  npx wrangler pages deploy public
